import React from 'react';
import '../../styles/vibes.css';

// Hero épuré pour mettre en avant le header
export default function Hero() {
  return (
    <section
      className="hero-root"
      style={{ background: 'linear-gradient(180deg, #0f172a, #0b1220)' }}
    >
      {/* Contenu supprimé temporairement pour clarté */}
    </section>
  );
}
